{
  "name": "json2csv",
  "description": "Converts Json to Comma Separated Values",
  "version": "0.9.0",
  "keywords": [
    "json",
    "csv"
  ],
  "dependencies": {
    "mocha": ">=1.13.0"
  },
  "scripts": {
    "test": "mocha --recursive --reporter spec"
  }
}